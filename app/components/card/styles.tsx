import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        flex:1,
        borderColor: "white",
        borderWidth: 1,
        borderRadius: 5,
        padding: 10,
        overflow: "hidden",
    },
    title: {
        fontSize: 16,
        color: "white",
        fontWeight: "bold",
    },
    textContent: {
        color: "white",
    }
});

export default styles;